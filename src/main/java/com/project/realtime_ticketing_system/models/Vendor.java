package com.project.realtime_ticketing_system.models;

public class Vendor extends UserTemplate{

    public Vendor(Long id, String name, String email, String passwordHash) {
        super(id, name, email, passwordHash);
    }
}
